<?php
  //connection au serveur
  // $cnx = mysql_connect( "localhost", "root", "" ) ;
 
  // //sélection de la base de données:
  // $db  = mysql_select_db( "cinema_v2" ) ;
 
  // //récupération des valeurs des champs:
  
  // $role   = $_POST["role"] ;
//   namespace Insertion; // namespace, permet de dire "je travaille dans ce dossier".

//   use Model\Connect; // use, permet d'importer une class d'un autre "dossier"
   
//   class CinemaInsertion {

 
//   //création de la requête SQL:
//   $sql = "INSERT  INTO roles (id_role, nom_role, descrip_role)
//             VALUES ( '$id_role', '$nom_role', '$descrip_role') " ;
 
//   //exécution de la requête SQL:
//   $requete = mysql_query($sql) or die( mysql_error() ) ;
 
//   //affichage des résultats, pour savoir si l'insertion a marchée:
//   if($requete)
//   {
//     echo("L'insertion a été correctement effectuée") ;
//   }
//   else
//   {
//     echo("L'insertion à échouée") ;
//   }

// }
?>